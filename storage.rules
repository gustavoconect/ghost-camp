rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true; // Todos podem ver as fotos
      allow write: if request.auth != null; // Apenas admin pode subir ou apagar
    }
  }
}
